# Native Binary Packages

**⚠️ DEPRECATED: This directory is no longer used.**

Native binary packages have been moved to `ui/acp/native/` to better integrate
with the `@aaif/goose-acp` package and enable `npm link` workflows.

Please see `ui/acp/README.md` for the new build and publish process.
